interface:
  display_name: "WebMCP Workflow CLI"
  short_description: "Run WebMCP workflow JSON from terminal"
  default_prompt: "Use $webmcp-workflow-cli to validate and run a WebMCP workflow JSON through `webmcp-workflow`."
