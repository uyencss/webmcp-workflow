{
  "name": "workflow-dispatcher",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for validating and executing WebMCP workflow JSON through a gateway.",
  "bin": {
    "workflow-dispatcher": "bin/workflow-dispatcher.js"
  },
  "scripts": {
    "start": "node bin/workflow-dispatcher.js",
    "test": "node --test tests/*.test.js",
    "test:workflow": "node tests/run-workflow-smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
