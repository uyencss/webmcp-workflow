{
  "defaultGateway": "local",
  "gateways": {
    "local": {
      "apiUrl": "http://localhost:7865/api",
      "healthUrl": "http://localhost:7865/health",
      "healthTimeoutMs": 3000,
      "defaultProfile": "",
      "profiles": {
        "personal": "replace-with-profile-id",
        "work": "replace-with-profile-id"
      }
    }
  },
  "defaults": {
    "timeoutMs": 30000,
    "strict": false,
    "allowUnknownCommand": false,
    "historyDir": ".workflow-runs",
    "redactKeys": ["token", "password", "cookie", "authorization", "apiKey"]
  },
  "workflows": {
    "example-title": {
      "path": "tests/fixtures/example-title-workflow.json",
      "gateway": "local",
      "description": "Open example.com and read the page title",
      "variables": {},
      "timeoutMs": 30000,
      "queue": {
        "lockKey": "gateway:local:profile:auto",
        "allowOverlap": false
      },
      "schedule": {
        "enabled": false,
        "intervalMs": 300000,
        "maxConsecutiveFailures": 5
      }
    }
  }
}
